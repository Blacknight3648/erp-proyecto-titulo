# Detalle de Escenarios de Uso Realizados en las Pruebas

Este documento detalla exhaustivamente los escenarios de prueba unitaria y de integración ejecutados sobre el backend Spring Boot del sistema ERP de Antuan SA, organizados bajo la estructura de la Matriz de QA e indicando los criterios de aceptación y resultados obtenidos.

## Registro Detallado de Escenarios de Pruebas (Backend)

| ESCENARIO / FUNCIONALIDAD | ID | BACKGROUND (Contexto / Descripción general) | ESCENARIO (Caso de Uso) | CRITERIO DE ACEPTACIÓN o DESCRIPCIÓN DE LA PRUEBA | RESULTADO |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Gestión de Usuarios - Servicio** | **CP-US-001** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Crear usuario exitosamente con DTO | Dado un DTO de creación de usuario con RUN válido, email único y roles/áreas válidos, se valida, mapea, guarda en la base de datos y se retorna el usuario. | **EXITOSO** (Retorna usuario con datos correctos y estado habilitado) |
| **Gestión de Usuarios - Servicio** | **CP-US-002** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Crear usuario con rol inexistente lanza excepción | Dado un DTO con rol no registrado en BD, al intentar crearlo se debe lanzar una excepción de negocio. | **EXITOSO** (Lanza `RuntimeException: Rol no encontrado`) |
| **Gestión de Usuarios - Servicio** | **CP-US-003** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Crear usuario con área inexistente lanza excepción | Dado un DTO con área no registrada en BD, al intentar crearlo se debe lanzar una excepción de negocio. | **EXITOSO** (Lanza `RuntimeException: Área no encontrada`) |
| **Gestión de Usuarios - Servicio** | **CP-US-004** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Crear usuario con email duplicado lanza `UserDuplicadoException` | Dado un DTO con un correo que ya existe en BD, al intentar registrar se lanza excepción de duplicidad. | **EXITOSO** (Lanza `UserDuplicadoException`) |
| **Gestión de Usuarios - Servicio** | **CP-US-005** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Listar usuarios retorna lista completa | Al solicitar el listado de usuarios, se debe retornar una lista que contenga todos los usuarios almacenados en la BD. | **EXITOSO** (Retorna lista con 3 usuarios registrados) |
| **Gestión de Usuarios - Servicio** | **CP-US-006** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Obtener usuario por ID existente | Dado un ID de usuario registrado (1L), al buscarlo se retorna el objeto del usuario correspondiente. | **EXITOSO** (Retorna usuario con ID=1L) |
| **Gestión de Usuarios - Servicio** | **CP-US-007** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Obtener usuario por ID inexistente lanza `UserNotFoundException` | Dado un ID no registrado (999L), al buscar por ID se debe lanzar una excepción de entidad no encontrada. | **EXITOSO** (Lanza `UserNotFoundException`) |
| **Gestión de Usuarios - Servicio** | **CP-US-008** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Obtener usuario por RUN existente | Dado un RUN de usuario registrado ("12345678-5"), al buscar por RUN se retorna el usuario correcto. | **EXITOSO** (Retorna usuario con RUN "12345678-5") |
| **Gestión de Usuarios - Servicio** | **CP-US-009** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Obtener usuario por RUN inexistente lanza excepción | Dado un RUN no registrado ("99999999-9"), al buscar por RUN se lanza una excepción de entidad no encontrada. | **EXITOSO** (Lanza `UserNotFoundException`) |
| **Gestión de Usuarios - Servicio** | **CP-US-010** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Obtener usuario por email existente | Dado un email de usuario registrado ("juan@mail.com"), al buscar por email se retorna el usuario correcto. | **EXITOSO** (Retorna usuario con email "juan@mail.com") |
| **Gestión de Usuarios - Servicio** | **CP-US-011** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Actualizar usuario parcialmente (solo nombre y teléfono) | Dado un usuario existente y datos de actualización parcial (nombre y teléfono nuevos, resto nulos), se guardan los cambios solo de los campos provistos. | **EXITOSO** (Conserva RUN/email originales y actualiza nombre y fono) |
| **Gestión de Usuarios - Servicio** | **CP-US-012** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Eliminar usuario existente desvincula vendedor y registros comerciales | Dado un usuario con rol de vendedor asociado a cotizaciones u órdenes, al eliminarlo se desvinculan sus registros y se borra el usuario/vendedor en BD. | **EXITOSO** (Desvincula EVN, NV, SCOS, SCot en cascada controlada) |
| **Gestión de Usuarios - Servicio** | **CP-US-013** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Eliminar usuario inexistente lanza excepción | Dado un ID de usuario inexistente, al intentar eliminarlo se debe lanzar la excepción de no encontrado. | **EXITOSO** (Lanza `UserNotFoundException` y no elimina nada) |
| **Gestión de Usuarios - Servicio** | **CP-US-014** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Toggle enabled cambia estado de habilitado | Al invocar la alternancia del estado de habilitación, se invierte el valor del atributo `enabled` en BD (true a false o false a true). | **EXITOSO** (Cambia de enabled=true a false, y viceversa) |
| **Gestión de Usuarios - Servicio** | **CP-US-015** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Asignar roles a usuario existente | Dado un usuario y una lista de nombres de roles válidos, se actualizan sus roles asignados y se persiste. | **EXITOSO** (Asigna roles y retorna usuario actualizado) |
| **Gestión de Usuarios - Servicio** | **CP-US-016** | Clase `UserServiceImpl`, dependencias mockeadas con Mockito | Asignar áreas a usuario existente | Dado un usuario y una lista de nombres de áreas válidas, se actualizan sus áreas asignadas y se persiste. | **EXITOSO** (Asigna áreas y retorna usuario actualizado) |
| **Gestión de Usuarios - Validador** | **CP-UV-001** | Clase `UserValidator`, dependencias mockeadas con Mockito | Validar unicidad de email existente lanza excepción | Dado un correo electrónico ya ocupado en la BD, la validación debe lanzar excepción de duplicidad. | **EXITOSO** (Lanza `UserDuplicadoException`) |
| **Gestión de Usuarios - Validador** | **CP-UV-002** | Clase `UserValidator`, dependencias mockeadas con Mockito | Validar unicidad de RUN existente lanza excepción | Dado un RUN ya ocupado en la BD, la validación de unicidad debe lanzar excepción de duplicidad. | **EXITOSO** (Lanza `UserDuplicadoException` indicando el RUN) |
| **Gestión de Usuarios - Validador** | **CP-UV-003** | Clase `UserValidator`, dependencias mockeadas con Mockito | Validar RUN inválido lanza `IllegalArgumentException` | Dado un RUN chileno con dígito verificador matemáticamente erróneo ("12345678-0"), se debe lanzar excepción de argumento ilegal. | **EXITOSO** (Lanza `IllegalArgumentException`) |
| **Gestión de Usuarios - Validador** | **CP-UV-004** | Clase `UserValidator`, dependencias mockeadas con Mockito | Validar unicidad con datos únicos pasa sin excepción | Dado un email y RUN que no existen previamente en la base de datos, la validación de unicidad no debe levantar fallos. | **EXITOSO** (Ejecución silenciosa sin excepciones) |
| **Gestión de Usuarios - Mapper** | **CP-UM-001** | Clase `UserMapper` | Mapear User a UserDTO correctamente | Dado un objeto de entidad `User` completo, se mapean todos sus campos a `UserDTO`, omitiendo password por seguridad. | **EXITOSO** (Mapea campos y password queda nulo en DTO) |
| **Gestión de Usuarios - Mapper** | **CP-UM-002** | Clase `UserMapper` | Mapear null a UserDTO retorna null | Si la entidad de entrada es nula, al transformarla a DTO se debe retornar null directamente sin excepciones. | **EXITOSO** (Retorna `null`) |
| **Gestión de Usuarios - Mapper** | **CP-UM-003** | Clase `UserMapper` | Mapear CreateUserDTO a User | Dado un DTO de creación de usuario, se mapea a entidad `User` fijando enabled en true y sin ID de BD asignado. | **EXITOSO** (Entidad User con enabled=true e ID nulo) |
| **Gestión de Usuarios - Mapper** | **CP-UM-004** | Clase `UserMapper` | Mapear CreateUserDTO null retorna null | Si el DTO de entrada es nulo, al transformarlo se debe retornar null directamente. | **EXITOSO** (Retorna `null`) |
| **Gestión de Usuarios - Controlador** | **CP-UC-001** | Controlador `UsuarioController` REST API, MockMvc mockea peticiones HTTP | GET /api/v1/usuarios retorna lista de usuarios | Se realiza petición HTTP GET y el controlador debe retornar código de estado 200 y el cuerpo con la lista en JSON. | **EXITOSO** (Retorna HTTP 200 con lista de DTOs) |
| **Gestión de Usuarios - Controlador** | **CP-UC-002** | Controlador `UsuarioController` REST API, MockMvc mockea peticiones HTTP | GET /api/v1/usuarios/{id} retorna usuario por ID | Se realiza petición HTTP GET con ID válido, el controlador responde HTTP 200 y el JSON del DTO correspondiente. | **EXITOSO** (Retorna HTTP 200 con el DTO del usuario) |
| **Gestión de Usuarios - Controlador** | **CP-UC-003** | Controlador `UsuarioController` REST API, MockMvc mockea peticiones HTTP | POST /api/v1/usuarios crea usuario y retorna 201 | Se realiza petición HTTP POST con DTO válido, responde HTTP 201 y los datos del usuario creado. | **EXITOSO** (Retorna HTTP 201 CREATED) |
| **Gestión de Usuarios - Controlador** | **CP-UC-004** | Controlador `UsuarioController` REST API, MockMvc mockea peticiones HTTP | DELETE /api/v1/usuarios/{id} retorna 204 | Se realiza petición HTTP DELETE para ID existente, el controlador responde código 204 sin contenido. | **EXITOSO** (Retorna HTTP 204 NO CONTENT) |
| **Gestión de Usuarios - Controlador** | **CP-UC-005** | Controlador `UsuarioController` REST API, MockMvc mockea peticiones HTTP | PATCH /api/v1/usuarios/{id}/toggle-enabled retorna 200 | Se realiza petición HTTP PATCH a toggle-enabled de usuario, responde HTTP 200 y el DTO con el estado cambiado. | **EXITOSO** (Retorna HTTP 200 con DTO modificado) |
| **Shared - Value Object** | **CP-MO-001** | Clase de Dominio `Money` | Crear Money con valores válidos | Dado un monto decimal y código de moneda ("CLP"), se inicializa el objeto conservando inmutabilidad. | **EXITOSO** (Objeto Money instanciado correctamente) |
| **Shared - Value Object** | **CP-MO-002** | Clase de Dominio `Money` | Crear Money con monto null lanza excepción | Al intentar instanciar un importe con valor nulo para el monto, se debe rechazar la creación. | **EXITOSO** (Lanza `IllegalArgumentException`) |
| **Shared - Value Object** | **CP-MO-003** | Clase de Dominio `Money` | Crear Money con moneda vacía lanza excepción | Al intentar instanciar un importe con código de moneda vacío o nulo, se debe rechazar la creación. | **EXITOSO** (Lanza `IllegalArgumentException`) |
| **Shared - Value Object** | **CP-MO-004** | Clase de Dominio `Money` | Sumar Money de misma moneda | Dado dos objetos Money con la misma divisa ("CLP"), al sumarlos se genera un nuevo Money con el valor acumulado. | **EXITOSO** (Retorna nuevo Money de 1500 CLP) |
| **Shared - Value Object** | **CP-MO-005** | Clase de Dominio `Money` | Sumar Money de distinta moneda lanza excepción | Al intentar sumar un importe en CLP con otro en USD, la operación debe ser rechazada. | **EXITOSO** (Lanza `IllegalArgumentException` por divisa distinta) |
| **Shared - Value Object** | **CP-MO-006** | Clase de Dominio `Money` | Multiplicar Money por entero | Al multiplicar un Money por un entero positivo, se calcula el nuevo monto conservando la misma divisa. | **EXITOSO** (Retorna nuevo Money multiplicado) |
| **Shared - Value Object** | **CP-MO-007** | Clase de Dominio `Money` | Multiplicar Money por negativo lanza excepción | Si se intenta multiplicar el dinero por un número negativo, la operación debe ser rechazada. | **EXITOSO** (Lanza `IllegalArgumentException`) |
| **Shared - Value Object** | **CP-MO-008** | Clase de Dominio `Money` | Money.zero crea instancia con monto cero | Dado un código de moneda, genera una constante con monto 0.00 en esa divisa. | **EXITOSO** (Retorna Money de 0 en la moneda indicada) |
| **Shared - Validación** | **CP-RV-001** | Clase de Dominio `RunValidator` | RUN válido con guión retorna true | Dado un RUN bien calculado ("12345678-5"), al validarlo retorna verdadero. | **EXITOSO** (Retorna `true`) |
| **Shared - Validación** | **CP-RV-002** | Clase de Dominio `RunValidator` | RUN válido con puntos y guión retorna true | Dado un RUN con formato completo de puntos y guión ("12.345.678-5"), retorna verdadero. | **EXITOSO** (Retorna `true`) |
| **Shared - Validación** | **CP-RV-003** | Clase de Dominio `RunValidator` | RUN con dígito verificador incorrecto retorna false | Dado un RUN con DV inválido ("12345678-0"), la validación debe rechazarlo. | **EXITOSO** (Retorna `false`) |
| **Shared - Validación** | **CP-RV-004** | Clase de Dominio `RunValidator` | RUN null retorna false | Una entrada nula de RUN debe considerarse inválida. | **EXITOSO** (Retorna `false`) |
| **Shared - Validación** | **CP-RV-005** | Clase de Dominio `RunValidator` | RUN vacío retorna false | Una entrada vacía de RUN debe considerarse inválida. | **EXITOSO** (Retorna `false`) |
| **Shared - Validación** | **CP-RV-006** | Clase de Dominio `RunValidator` | RUN con letras en cuerpo retorna false | Si el cuerpo numérico del RUN contiene caracteres alfabéticos, se rechaza la validación. | **EXITOSO** (Retorna `false`) |
| **Shared - Validación** | **CP-RV-007** | Clase de Dominio `RunValidator` | RUN con K como dígito verificador válido | Dado un RUN chileno cuyo DV calculado es 'K' ("12345678-K"), la validación debe aceptarlo. | **EXITOSO** (Retorna `true`) |
| **Comercial - Dominio** | **CP-NV-001** | Clase de Dominio `NotaVenta` | Crear NotaVenta con estado inicial BORRADOR | Una Nota de Venta nueva se debe inicializar siempre en estado BORRADOR y con fecha actual. | **EXITOSO** (NotaVenta creada con estado BORRADOR) |
| **Comercial - Dominio** | **CP-NV-002** | Clase de Dominio `NotaVenta` | Emitir NotaVenta en estado BORRADOR | Al invocar `emitir()` en una Nota de Venta en estado BORRADOR, su estado debe cambiar a EMITIDA. | **EXITOSO** (Estado cambia a EMITIDA) |
| **Comercial - Dominio** | **CP-NV-003** | Clase de Dominio `NotaVenta` | Emitir NotaVenta ya emitida lanza excepción | Intentar emitir una Nota de Venta que ya está emitida debe lanzar excepción por transición ilegal. | **EXITOSO** (Lanza `IllegalStateException`) |
| **Comercial - Dominio** | **CP-NV-004** | Clase de Dominio `NotaVenta` | Cancelar NotaVenta en BORRADOR | Al invocar `cancelar()` en una Nota de Venta en estado BORRADOR, su estado debe cambiar a CANCELADA. | **EXITOSO** (Estado cambia a CANCELADA) |
| **Comercial - Dominio** | **CP-NV-005** | Clase de Dominio `NotaVenta` | Cancelar NotaVenta COMPLETADA lanza excepción | Si la Nota de Venta ya está en estado COMPLETADA, no puede ser cancelada, lanzando excepción. | **EXITOSO** (Lanza `IllegalStateException`) |
| **Comercial - Dominio** | **CP-NV-006** | Clase de Dominio `NotaVenta` | Calcular totales con ítems (IVA 19%) | Dado una Nota de Venta con ítems cuyo valor neto suma 10000 CLP, se debe calcular subtotal 10000, IVA 1900, y Total 11900. | **EXITOSO** (Montos coinciden exactamente con IVA del 19%) |
| **Comercial - Enum** | **CP-ENV-001** | Enum `EstadoNV` | EstadoNV tiene los valores esperados | El enum `EstadoNV` debe contener exactamente los 6 estados del flujo comercial. | **EXITOSO** (Contiene BORRADOR, EMITIDA, EN_PRODUCCION, COMPLETADA, ENTREGADA, CANCELADA) |
| **Comercial - Enum** | **CP-ENV-002** | Enum `EstadoNV` | EstadoNV descripción correcta | Cada estado del enum debe poseer una descripción en texto legible en español para presentación en UI. | **EXITOSO** (Retorna descripciones correctas en español) |
| **Producción - Dominio** | **CP-OC-001** | Clase de Dominio `OrdenCompra` | Emitir OrdenCompra con estado inicial EMITIDA | Al emitir una Orden de Compra, su estado debe ser EMITIDA, fecha de emisión hoy, y total neto cero. | **EXITOSO** (Estado inicial de OC es EMITIDA) |
| **Producción - Dominio** | **CP-OC-002** | Clase de Dominio `OrdenCompra` | Transición EMITIDA → ENVIADA es válida | Al despachar la OC, el estado debe cambiar de EMITIDA a ENVIADA al llamar a `marcarEnviada()`. | **EXITOSO** (Estado cambia a ENVIADA) |
| **Producción - Dominio** | **CP-OC-003** | Clase de Dominio `OrdenCompra` | Transición EMITIDA → RECEPCIONADA es inválida | Intentar recepcionar una OC directamente desde EMITIDA (sin enviarla) debe ser rechazado con excepción. | **EXITOSO** (Lanza `IllegalStateException`) |
| **Producción - Dominio** | **CP-OC-004** | Clase de Dominio `OrdenCompra` | Transición ENVIADA → RECEPCIONADA es válida | Al recibir mercadería de una OC previamente enviada, el estado transiciona a RECEPCIONADA. | **EXITOSO** (Estado cambia a RECEPCIONADA) |
| **Producción - Dominio** | **CP-OC-005** | Clase de Dominio `OrdenCompra` | Transición RECEPCIONADA → CERRADA es válida | Al cerrar una OC que ya fue recepcionada, el estado debe transicionar a CERRADA. | **EXITOSO** (Estado cambia a CERRADA) |
| **Producción - Dominio** | **CP-OC-006** | Clase de Dominio `OrdenCompra` | Cerrar OC ya CERRADA lanza excepción | Intentar cerrar una Orden de Compra que ya fue cerrada debe lanzar excepción de estado ilegal. | **EXITOSO** (Lanza `IllegalStateException`) |
| **Producción - Dominio** | **CP-OC-007** | Clase de Dominio `OrdenCompra` | Recalcular total neto de OC con ítems | Al añadir ítems a la OC con subtotales de 5000 y 3000, el total neto de la OC se actualiza a 8000. | **EXITOSO** (Total neto se recalcula automáticamente a 8000) |
| **Producción - Dominio** | **CP-OC-008** | Clase de Dominio `OrdenCompra` | Lista de ítems es inmutable | La colección de ítems obtenida con `getItems()` debe ser de sólo lectura para proteger el encapsulamiento. | **EXITOSO** (Lanza `UnsupportedOperationException` al intentar añadir ítems directamente) |
| **Producción - Enum** | **CP-EOC-001** | Enum `EstadoOC` | Transiciones válidas de EMITIDA | Desde el estado EMITIDA, la única transición directa válida permitida debe ser a ENVIADA. | **EXITOSO** (Sólo permite transición a ENVIADA) |
| **Producción - Enum** | **CP-EOC-002** | Enum `EstadoOC` | Transiciones válidas de ENVIADA | Desde el estado ENVIADA, se debe permitir transicionar a RECEPCIONADA o RECEPCIONADA_PARCIAL. | **EXITOSO** (Permite transiciones de recepción, rechaza el resto) |
| **Producción - Enum** | **CP-EOC-003** | Enum `EstadoOC` | CERRADA no puede transicionar a ningún estado | El estado CERRADA es final; ninguna transición posterior debe ser permitida. | **EXITOSO** (Retorna false para cualquier estado de destino) |
| **Producción - Enum** | **CP-EOC-004** | Enum `EstadoOC` | Transición con destino null retorna false | Al verificar la transición desde cualquier estado hacia un destino nulo, se debe retornar false. | **EXITOSO** (Retorna false sin excepciones) |
| **Producción - Enum** | **CP-EOC-005** | Enum `EstadoOC` | RECEPCIONADA solo puede transicionar a CERRADA | Desde el estado RECEPCIONADA, la única transición posterior válida permitida debe ser a CERRADA. | **EXITOSO** (Sólo permite transición a CERRADA) |
